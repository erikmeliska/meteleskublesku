import MoviePage from "../../page";

/**
 * /movie/[id]/clip/[clipId]
 * Renders the movie page with the specified clip selected.
 * The clip ID maps to an index in the track list.
 */
export { generateMetadata } from "../../page";

export default MoviePage;
